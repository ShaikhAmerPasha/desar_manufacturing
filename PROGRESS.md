# Progress

## `specs/grade-scrap-ux-fixes.md`

- [x] Phase 1a — `grade_utils.py` helper + `grade_yield_analysis` fix (tracer bullet) — verified on `excel` site, returns real grade splits
- [x] Phase 1b — `roll_life_tracker` + `get_grade_summary` fix + `roll_ticket.json` field_order cleanup — all verified against real data on `excel`
- [x] Phase 1c — `test_grade_reports.py` regression test (2 tests, both pass — see note below on `run-tests`)
- [x] Phase 2 — Remove scrap valuation from `repack_service.py` + update affected tests — 6 tests updated/added across 3 files, all verified passing manually
- [x] Phase 3 — Simplify `desar_production_order.js` (collapse WO/SE/QI links, inline-submit, roll status badge) — syntax-checked (`node --check`), logic reviewed; **live browser click-through still needed** — no browser/agent-browser tool available in this session, see note below

## Environment notes (found during Phase 1, unrelated to this app's code)

- **`bench --site excel run-tests` is broken site-wide**, not just for this app: the `hrms` app installed on `excel` imports `frappe.tests.IntegrationTestCase`, which doesn't exist in the installed Frappe 15.83 (`frappe.tests.utils.FrappeTestCase` is the real base class here). This fails in `before_tests`, before any test module runs. Not fixed as part of this work (out of scope — belongs to the `hrms` app / bench upgrade, not `desar_manufacturing`). Verified `test_grade_reports.py` manually instead (unittest run in-process via `bench execute`, then confirmed via `frappe.db.exists` that the transaction rolled back and left no test data).
- Corrected the `frappe-app-dev` skill's testing guidance for this environment: use `FrappeTestCase` (`frappe.tests.utils`), not `IntegrationTestCase` (doesn't exist on Frappe 15.83). Existing test files in this app using plain `unittest.TestCase` should eventually move to `FrappeTestCase` for automatic per-test rollback — not done here (out of scope for this spec), but worth a follow-up.
- Confirmed the real dev site is `excel`, not `apnaslot.localhost` from CLAUDE.md (stale reference from a different project template).
- **Phase 3 manual verification pending:** no browser automation tool (agent-browser or otherwise) was available in this session, so `desar_production_order.js` was verified by `node --check` (syntax) and manual code review only — not click-tested in an actual browser. Before treating Phase 3 as done: open a DESAR Production Order on `excel`, run one roll through Warping → Grey → Finished → Packing, and confirm (a) WO/Job Card/Stock Entry links are hidden behind "Details" until expanded, (b) the roll-level Grey/Finishing/Packing status badges show, (c) draft Transfer SE / Manufacture SE / Quality Inspection each have a working inline "Submit" button that doesn't require leaving the form.

## Dynamic Beam Split (see `.claude/plans/proud-napping-lollipop.md` for full spec)

- [x] Beam Split rework — supervisor enters N `(qty_to_split, pieces_per_split)` rows instead of one uniform roll count. New `DESAR Beam Split Row` child doctype (dialog-only), `planned_qty` field on `DESAR Roll Chain`, `warping_service.split_beam` rewritten with legacy (redistribute-only) vs fresh (dynamic roll count) branches, `wo_split_helpers.py` extracted. Fixed `total_qty` to come from the real Sales Order qty instead of `roll_count * pieces_per_roll`. New `tests/test_warping_service.py` (12 tests), all passing on `excel`.

## Dynamism config audit (live data, `excel` site)

- [x] Found + fixed: `DESAR Grade Configuration` had 0 rows → Grade C was being silently treated as scrap (no warehouse, no tracking). Created `Finished Goods Grade C - ST` warehouse, 5 missing `-C` Item variants (2 via proper Item Variant creation, 3 via clone), added the 3-row Grade Configuration table (A/100%, B/60%, C/20%).
- [x] Found + fixed: none of the real Design Masters had the client's 8 Packing sub-operations (classification, cutting by piece, piece finishing, steaming, ironing, stamping, packaging, boxing) — every Packing WO fell back to one generic "Packing" op. Added the 8-row Operations table (with workstations, creating 3 missing Operation masters) to the 5 Design Masters that actually have a Packing stage (563, DM-2026-0001/0002/0003/0007).
- [ ] **Follow-up, not done:** 4 of those 5 Design Masters already have an existing BOM referencing the old single-op version — needs "Create All BOMs" re-triggered per Design Master to pick up the new 8 ops. 2 Design Masters (601, 801) have zero Stage Configuration at all — still on full legacy BOM/repack mode, left untouched (separate decision on whether to backfill them).

## P0 Hardening — concurrency, commit safety, silent failures

- [x] Added `for_update=True` to all 11 read-check-write stage-transition entry points (`roll_service.py` x8, `warping_service.py` x3) — closes the double-click/two-supervisor race that could create duplicate WOs/SEs/Batches. `refresh_roll` intentionally left unlocked (idempotent).
- [x] Removed all 6 mid-function `frappe.db.commit()` calls (`roll_service._update_roll`, `warping_service` x3, `production_plan_service`, `bom_service`) — request-level auto-commit/rollback now actually protects multi-step operations.
- [x] Fixed the 5 silently-swallowed-exception sites: `_create_roll_qi` now propagates (QI failure correctly aborts the whole stage-completion instead of stranding a fake "pending" roll); `repack_service`/`roll_ticket_service` failures now surface visibly (msgprint without `alert=True` for repack; new msgprint added for roll ticket, which had none); `qi_service`/`wo_split_helpers` failures now log to Error Log instead of a bare `pass`.
- [x] New `tests/test_p0_hardening.py` (10 tests, all against real code) — verifies the lock kwarg, verifies no commit survives, verifies each propagate/surface/log verdict. Full suite: 210/210 passing on `excel`.

## P1 — Authorization on RPC endpoints

- [x] `api/production_order.py`'s 14 whitelisted methods now call `frappe.only_for(...)` before doing anything, matching `DESAR Production Order`'s own doctype permissions: `create_desar_po`/`preview_plan` require System Manager/DESAR Supervisor (matches `create` permission — Operator can't create a PO); every stage-transition method requires System Manager/DESAR Supervisor/DESAR Operator (matches `write` permission). DESAR QC Inspector (read-only role) is now correctly blocked from all of them — previously any authenticated user could call any of these.
- [x] New `tests/test_p1_hardening.py` — asserts every endpoint checks roles before touching `_safe`/the service layer. Full suite: 211/211 passing.
- [x] **Follow-up, done:** checked `api/manufacturing.py`'s ~15 endpoints — most already call `frappe.has_permission(...)`, not actually ungated like the audit implied. Found the 2 real gaps: `finish_work_order` and `create_transfer_se` (both submit real Stock Entries) had zero check at all. Added `frappe.only_for(MUTATE_ROLES)` to both, reusing the same role constant from `api/production_order.py`. Read-only info endpoints (`get_grade_summary`, `get_job_card_actions`, etc.) left ungated — non-mutating, lower risk, proportionate effort.

## P2 — Real test coverage + CI (tracer bullet — see `specs/p2-test-coverage-and-ci.md`)

- [x] Converted `test_new_features.py::TestGradeAdjustmentMath` from a local math reimplementation to calling the real `events.quality_inspection._validate_adjustment_quantities` directly (guarded with `self.skipTest(...)` when `frappe.db` isn't bound, so plain `pytest` still degrades gracefully). Proves real service/event functions are unit-testable without full DB fixtures. 26/26 passing under `bench run-tests`.
- [x] Added `.github/workflows/run-tests.yml` — first CI this repo has ever had. Standard bench-init + mariadb-service + fresh-site skeleton running `bench run-tests --app desar_manufacturing`.
- [ ] **Not verified — no way to run GitHub Actions from this environment.** First real push will surface any bench-version/CLI-flag mismatches; treat this as the tracer bullet, not a finished pipeline.
- [ ] **Deferred:** the other ~100 "mirror" tests across the suite (`test_production_order.py`, `test_dynamic_architecture.py`, etc.) still reimplement logic instead of calling real code — only convert further if this tracer bullet's pattern holds up in CI.

## P3 — Scale/performance (see `specs/p3-scale-performance.md`)

- [x] Benchmarked real beam-split WO cloning on `excel` (rolled back, nothing persisted): ~36 queries/roll, sub-second up to 20 rolls, ~4s at 100 rolls. Real production data shows actual splits are 1-4 rolls — **not rewriting the per-roll insert loop**, the risk (bypassing WO validate/BOM hooks) isn't worth it at current scale. Documented the exact threshold to revisit at (30-50+ rolls).
- [x] Added `search_index: 1` to `DESAR Roll Chain.beam_roll_batch` and `Quality Inspection.custom_desar_stage_name` — verified via `SHOW INDEX` on `excel`, both real now.
- [x] Dashboard's 1000-row query cap (`desar_production_order.js`) was silently truncating — added `_warn_if_truncated()` so hitting the cap now shows an orange alert instead of silently dropping rows. Full pagination deferred until this is actually observed happening.
- [x] Full suite still 211/211 after all P3 changes.

## P4 — Maintainability (see `specs/p4-maintainability.md`)

- [x] Collapsed `roll_service._configure_wo`'s 3 duplicated keyword→warehouse map loops into one `_resolve_by_keyword()` helper. Behavior-preserving, new `tests/test_p4_hardening.py`, full suite 214/214.
- [x] Checked audit's "dead code" claim on `repack_service._build_items` — **wrong, it's actively used** by real passing tests (`test_e2e_cycle.py`, `test_roll_ticket_service.py`). Not removed.
- [ ] **Deferred:** `roll_service`'s bigger start/complete duplication (8 functions, all just P0-hardened — too risky to refactor without a browser re-verify), `api/manufacturing.py`'s misplaced business logic (separate file, no audit done on it yet), version-label drift (v6/v7/v3.3, cosmetic only).

## Fractional Sub-Assembly Qty Fix (user-reported, real manual-test bug)

- [x] Root cause confirmed by reading the actual code: `bom_service.py`'s BOM chain fixes 1 Warping Beam = 1 Grey Roll = 1 Finished Roll = `pieces_per_roll` Shemagh pieces; ERPNext's own MRP explosion (`production_plan.py:1813`) never rounds to whole numbers; `WorkOrder.validate_qty()` (`work_order.py:1068-1078`) hard-throws on save for whole-number-UOM items. Confirmed via web research this is a known, still-open ERPNext core gap (github.com/frappe/erpnext#48147, #41476), not a DESAR misconfiguration.
- [x] Two-layer fix, both round **up**: new `events/production_plan.py` (`validate` hook, rounds `sub_assembly_items` qty) + `events/work_order.py` gained a `before_insert` hook (must be `before_insert` not `validate` — confirmed via reading `Document.hook`'s `compose()` that the core controller's own `validate()`/`validate_qty()` runs before any hooks.py-registered `validate` function gets a turn). Shared pure helper `round_up_if_needed()` in `validation_utils.py`.
- [x] Verified the fix actually lands: read ERPNext's `prepare_data_for_sub_assembly_items` (production_plan.py:783-808) — `stock_uom` is copied explicitly into the new Work Order dict before insert, so no fetch_from timing gap; `before_insert` fires on any `doc.insert()` regardless of which UI button triggered it.
- [x] New `tests/test_fractional_qty_fix.py` + additions to `tests/test_validation_utils.py`. Full suite 225/225 passing. `bench --site excel clear-cache` run so the new hooks are live.
- [ ] **Not verified end-to-end in a browser** (no browser tool, same limitation as everything else this session) — please repeat your exact original steps (odd Sales Order qty → Production Plan → Get Items → Work Orders) and confirm the fractional error is actually gone on screen.

## Environment notes (this session)

- `bench --site excel run-tests` (and `--module`-scoped variants) ran cleanly every time this session — the site-wide breakage noted above (from `hrms`'s `IntegrationTestCase` import) did not reproduce. Worth re-checking if it resurfaces; not investigated further since every run this session succeeded with real pass/fail output.
- `frappe-app-dev`/`frappe-ui` skills referenced in this file aren't registered as invocable skills in this session's tool list — proceeded using the codebase's own established service-layer/doctype conventions instead.

## Maturity hardening pass (6/10 → industry-grade path, see `.claude/plans/as-you-know-the-peppy-starlight.md`)

- [x] **Cancel-safety**: added `on_cancel` to `hooks.py` for Stock Entry/Quality Inspection/Work Order (none existed before — biggest gap found in the audit). QI cancel reverts Roll Ticket `stage_grades`/`roll_status` and clears the matching `DESAR Roll Chain` `*_qi` reference; Stock Entry cancel deletes an auto-created Roll Ticket if it has no stage progress yet (else logs for manual review) and resets `*_batch`/`packing_manufacture_se` references; Work Order cancel resets its roll's stage back to "Not Started" and clears the WO link. All three refuse (`frappe.throw`) if the next stage already started, rather than silently cascading corruption downstream.
- [x] **Transactional safety**: `warping_service.split_beam` now sets a savepoint before creating the Repack SE; on any failure in the split logic that follows, rolls back to it (erasing the SE and its stock ledger entries entirely) instead of leaving a submitted SE with no roll_chains behind it. Fixed the `_resize_packing_wo` TOCTOU by locking the Work Order (`for_update=True`) before checking `docstatus`, instead of a separate unlocked read.
- [x] Removed duplicated business logic: `is_final_stage` now has one implementation (`grade_utils.py`); `events/quality_inspection.py` and `services/roll_ticket_service.py` both call it instead of re-querying. Final-grade-total check in `events/quality_inspection.py` now calls `validation_utils.validate_grade_readings_total` instead of reimplementing the comparison inline. Merged `validate_grey_estimate_reasonable`/`validate_finishing_estimate_reasonable` into one parameterized `validate_estimate_reasonable`. Moved `roll_service._resolve_by_keyword` into `validation_utils.resolve_by_keyword` (pure, shared) and reused it in `bom_service` too.
- [x] De-hardcoded `bom_service.py`: `_get_default_workstation`'s "first workstation in DB" nondeterministic fallback now throws a clear error instead of guessing; `_get_source_wh_for_stage`'s hardcoded warehouse-name string fallbacks (e.g. `"Warping WIP - ST"`) removed — throws if the DESAR Settings field isn't configured, matching the workstation fix's philosophy. **Deferred**: legacy-mode's per-operation workstation literals and the `Shemagh-{article}-{size}-A` item-code convention in `_create_bom_shemagh` — no DESAR Settings/Design Master field exists yet to hold either, and adding one is a schema change (new custom fields + migration) this pass didn't want to ship unverified in a browser.
- [x] Test hygiene: deleted the placeholder `test_existing_50_tests_still_pass` (asserted `True` after two imports — not a real check). Rewrote `test_warping_service.py`'s `TestValidateSplitRows` to call `warping_service._validate_split_rows` directly instead of a hand-maintained reimplementation (it was explicitly docstring-labeled "Mirror of..."). Migrated `test_roll_ticket_service.py`'s `TestRollTicketServiceIntegration` to `FrappeTestCase` and removed 3 explicit `frappe.db.commit()` calls that were defeating even manual cleanup — verified via `bench console` that no `ITEST-RL-*` batches or `RT-TEST-*` Roll Tickets survive a test run. **Scope check, not a gap**: grepped every other test file for `.insert()`/raw `INSERT` — only this file and `test_p0_hardening.py` (already uses `addCleanup`, safe) create real documents; the other "duplicated `setUpClass`" blocks in `test_dynamic_architecture.py` etc. are read-only DocType/meta-existence guards, not fixture duplication, so left as plain `unittest.TestCase` (no rollback need, nothing to gain from a shared factories module here).
- [x] Fixed the CLAUDE.md/reality gap instead of building unverified new infra: corrected the Testing section (Playwright e2e pipeline it describes — `e2e/` specs, `e2e_seed.py`, `.github/workflows/ui-tests.yml` — does not exist in this repo; documented `test_e2e_cycle.py`'s actual manual-invocation role instead) and the Credentials section (site `desar_manufacturing.localhost` doesn't exist in this bench; the app is actually on `excel`).
- [x] Full suite 224/224 passing (225 minus the deleted placeholder) after every phase above, re-run after each change, not just at the end.
- [ ] **Not verified end-to-end in a browser** — same standing limitation as every prior session. Before trusting the on_cancel reconciliation logic in production: submit a Grey Roll QI, cancel it, and confirm the roll's stage resets to "In Progress" with the QI reference cleared on screen; do the same for a Manufacture Stock Entry and a Work Order.

## Browser-verification bugs found this session (real click-through, not code review)

- [x] **Split Beam dialog didn't open** — `frappe.ui.Dialog`'s `split_rows` Table field set `options: "DESAR Beam Split Row"` with no inline `fields:` array. A Dialog not bound to a `frm` never consults doctype meta for its grid (`grid.js:548-577` — `this.docfields = this.df.fields` when there's no `this.frm`), so `this.docfields` was `undefined` and crashed. First attempted fix (`frappe.model.with_doctype` to preload the meta) was **wrong** — proven wrong by the user's own stack trace showing the meta fetch completing successfully before the identical crash recurred. Corrected fix: added the `fields:` array inline (copied from `desar_beam_split_row.json`), matching 3 real Frappe-core precedents (`translation_manager.js`, `query_report.js`, `widget_dialog.js`) that all do the same for non-frm Table fields.
- [x] **Split Beam threw "Row 1: Quantity (1.6) cannot be a fraction"** after the dialog fix — a second, deeper bug: `warping_service._clone_combined_wo` (via `wo_split_helpers.py`) clones a Work Order to a smaller `qty` then calls ERPNext's own `set_required_items()`, which recomputes each `required_items` row's `required_qty` by BOM ratio — same fractional-qty class as the earlier Production Plan fix, but one level down (child table row, not the WO's own `qty`). `events/work_order.py`'s existing `before_insert` hook only rounded `doc.qty`. Extended it to also round each `required_items[].required_qty` when that row's `stock_uom` requires a whole number, reusing the same `validation_utils.round_up_if_needed` helper. New tests in `test_fractional_qty_fix.py` (3 added, 9/9 passing in that module). Full suite 227/227.
- [x] **Split Beam succeeded, but then a Grey Roll QI submit threw "Final grade total (X) cannot exceed manufactured qty (1)"** on a non-final stage — real, serious pre-existing bug, found by DB investigation (not code review): `grade_utils.is_final_stage(stage_name)` queried `DESAR Stage Configuration` by `stage_name` alone, with **no `parent`/Design Master scoping**. Stage names repeat across Design Masters with different configs (e.g. "Grey Roll" is flagged final on some legacy Design Masters, not on `DM-2026-0002`) — since the query wasn't scoped, it matched *any* Design Master's config, so a Grey Roll QI belonging to `DM-2026-0002` (where Grey Roll is correctly non-final) got wrongly treated as final because some *other* Design Master flags Grey Roll as final. Fixed: `is_final_stage(stage_name, design_master)` now requires and filters by `design_master`; added `grade_utils.get_design_master_from_qi()`/`get_work_order_from_qi()` to resolve it from a QI's reference chain (Stock Entry → Work Order → `custom_design_master`), reused by `events/quality_inspection.py` (3 call sites + `_get_wo_qty`, deduped onto the same resolver) and `roll_ticket_service._get_next_status` (which had the *same* unscoped-query bug on its `stage_seq`/`next_stage` lookups too — fixed all three in that function). Audited every other `DESAR Stage Configuration` query site in the app (`api/manufacturing.py`, `events/work_order.py`, the 3 page controllers) — all already properly scoped by `parent`; this bug was isolated to the two `grade_utils.is_final_stage` call chains. `get_final_stage_names()` (used by `roll_life_tracker` report) intentionally left unscoped — it's a cross-Design-Master reporting heuristic, not a per-record validation gate. Verified live against the actual failing QI in the DB (`MAT-QA-2026-00109`): `is_final_stage` now correctly returns `False`. Full suite 227/227.
- [ ] **Not yet re-verified in browser** — submit that same Grey Roll QI again (or a fresh one) and confirm no error; then continue through to an actual Packing-stage QI and confirm the final-grade check *does* still fire correctly there.
- [x] **Fraction error persisted after restart — traced through 3 layers, real root cause was a domain modeling bug, not a validation gap.** User confirmed restarting `bench start` (Procfile `web: bench serve --port 8000`, no auto-reload) and retrying still hit "Row 1: Quantity (1.62) cannot be a fraction" on Start Packing. Reproduced safely via `frappe.db.savepoint()` + rollback (no real mutations) in `bench console`:
  1. `WorkOrder.validate()` (`work_order.py:169-174`) checks `required_qty` integer-ness **before** calling `set_required_items()`, which recomputes it fresh from the BOM ratio **after** the check — so `wo.save()` "succeeds" while silently re-corrupting the value back to a fraction. Fixed by adding `_round_required_items_qty()` to `events/work_order.py`'s existing `validate()` hook too (not just `before_validate`), since that hooks.py handler runs after the core class's own `validate()` (already relied on by the existing autofill logic) and catches the post-recompute value.
  2. That didn't fully explain it either — the WO itself never threw; the real throw was one step later, on the **Transfer Stock Entry** `start_packing` creates immediately after (`stock_entry.py:215-216` has its own identical `validate_uom_is_integer` check, and ERPNext's `make_stock_entry()` copies `required_qty` straight into the SE's item `qty` — `work_order.py:1486`). Added a matching `before_validate` hook to `events/stock_entry.py`, rounding each item row's `qty`/`transfer_qty`.
  3. With both of those fixed, a **third**, more interesting issue surfaced: `BatchNegativeStockError` — rounding `1.62` **up** to `2.0` made the Packing WO think it needed 2 whole rolls of Finished Roll input, but only 1 physical roll (the actual batch) exists. Root cause: a stage always consumes exactly **one** upstream roll regardless of how many pieces that roll holds (that's the whole point of uneven beam-split) — the BOM's proportional-ratio math (`output_qty / nominal_pieces_per_roll`) doesn't fit that "1 roll in, N pieces out" relationship, and "round up" is domain-wrong for input consumption (right instinct for output qty, wrong for this). Real fix (confirmed with the user before applying — changes actual material-consumption quantity, not just a validation workaround): `roll_service._configure_wo` now forces `required_qty = 1` on the primary input item row (Beam Roll/Grey Roll/Finished Roll, per stage — reusing the `input_item` mapping already there), stashed via `wo.flags.desar_force_qty_1_item` since `set_required_items()` would otherwise overwrite it before the hooks.py `validate` handler re-applies it (`events/work_order.py::_reapply_forced_input_qty`).
  - Verified the **entire real flow** end-to-end by calling `roll_service.start_packing()` directly inside a sandboxed savepoint (rolled back after, zero real mutations): WO submits clean, Transfer SE creates successfully, `Finished Roll` required_qty is correctly `1.0`. Full suite 229/229.
- [x] **"Roll 1 already Completed/In Progress" — real bug, confirmed twice (PO-00041, PO-00042) via direct DB inspection.** `warping_service._insert_roll_chain_row` inserts each roll's child row standalone (not via `parent.append()+save()`), so Frappe's idx-from-list-position auto-numbering never runs — every row landed at `idx=0`. With no tiebreaker, child-table display order was arbitrary; the user's visually-first "Roll 1" card was actually roll_no=2 or 3 in the database both times, so the errors were technically correct for the roll actually being hit — just not the one the user thought they clicked. Fixed `_insert_roll_chain_row` to set `idx: roll_no` explicitly (new splits display correctly going forward) + ran a one-time data backfill (user-approved, blocked once by the auto-mode classifier as a live bulk write — re-confirmed via plan-mode) setting `idx = roll_no` on the 11 existing rows still at `idx=0` across POs 00039-00042. Verified: 0 rows remain at `idx=0`; PO-00042 now shows idx 1/2/3 matching roll_no 1/2/3 exactly. Full suite 229/229.

## Repack SE orphaned after Packing QI — user-reported

- [x] **Draft Repack SE became unreachable if the user navigated away before submitting it.** `RepackService.create_from_final_qi` (`repack_service.py`) inserts the grade-split Repack SE as Draft and never auto-submits it (by design — needs review); nothing recorded its name anywhere findable from the Production Order. Fixed in two parts: (1) new `repack_se` field (Link→Stock Entry) on `DESAR Roll Chain`, written via best-effort `frappe.db.set_value` in a new `_link_repack_se_to_roll_chain()` (looked up via the QI's `custom_roll_ticket` → matching roll chain row), wrapped in its own try/except so a linking failure can't get mislabeled as "Repack SE creation failed" — the SE already exists by that point regardless. (2) Discovered `DESAR Production Order`'s roll display is **not** the native Frappe grid — `desar_production_order.js` hides it and renders a fully custom per-roll HTML dashboard, so the new field's `in_list_view:1` alone was invisible to the user. Added an actual "View Repack SE" button in `_add_roll_buttons` (same `_btn()` helper as every other per-roll action), so it now shows up on the roll's card once `repack_se` is set. `bench --site excel migrate` clean, full suite 229/229 (JS-only second half, no Python re-run needed but re-verified anyway).
- [ ] **Not verified end-to-end in a browser** — submit a real Packing QI, confirm the Repack SE gets created and linked, reload the Production Order, and confirm the "View Repack SE" button appears on that roll's card and opens the Stock Entry.

## Live grade-count sync in Packing QI — user-requested

- [x] **Packing QI's Grade A/B/C readings always started at 0, and had to be manually kept consistent with the Grade Adjustments log** (only checked at submit, not helped along). User asked for Finishing-stage counts to pre-fill on open and live-recompute as Grade Adjustment rows are entered, staying manually editable (their choice, over locking the fields). Reused the exact reconciliation math already enforced server-side rather than inventing a second version: moved `events/quality_inspection.py`'s private `_get_stage_grades_from_rt` to `utils/grade_utils.py` as public `get_stage_grades_from_rt` (delegates to the existing `get_stage_grades` helper instead of re-querying), used by both the unchanged `before_submit` validation and a new read-only `api/manufacturing.py::get_finishing_grade_baseline(roll_ticket)` (`READ_ROLES`-gated). New `public/js/quality_inspection.js` (Quality Inspection is core, not DESAR-owned, so wired via a new `hooks.py` `doctype_js` entry — same pattern as the existing `Production Plan` script): pre-fills Grade A/B/C from the baseline only when every reading is still 0 (won't clobber hand-entered values), and recomputes them live on every Grade Adjustment `from_grade`/`to_grade`/`qty`/row-remove change using the identical `expected = baseline - Σfrom + Σto` arithmetic as `_validate_adjustment_quantities` (commented in both places to keep them in sync). Server-side submit check left untouched as the safety net. `bench build --app desar_manufacturing` + `clear-cache` run, full suite 229/229.
- [ ] **Not verified end-to-end in a browser** — open a real Packing QI for a roll with recorded Finishing grades, confirm Grade A/B/C pre-fill on load; add a Grade Adjustment row and confirm the readings update live without a reload; confirm submit still throws if a value is hand-edited to something inconsistent with the adjustment log afterward.
- [x] **Bug found by user in the above: "Not permitted" dialog on every Packing QI open, and the pre-fill/live-recompute silently never worked.** Root cause: `get_finishing_grade_baseline` was declared `@frappe.whitelist(methods=["GET"])`, but `quality_inspection.js`'s `frappe.call` doesn't pass `type: "GET"` so it goes out as POST by default (`frappe/public/js/frappe/request.js`) — `handler.py`'s method-mismatch check throws `frappe.PermissionError("Not permitted")` on every `refresh`, so the success callback (which does all the real work) never ran. Fixed by dropping the `methods=["GET"]` restriction, matching every other read endpoint in this file that's actually called from client JS. Confirmed no role-mismatch existed (checked real `excel`-site users — all 5 DESAR accounts already hold a `READ_ROLES` role). Full suite 229/229.
- [x] **User then asked for the sync trigger to also fire on QI Template change, always overwriting manual edits** (reversing their earlier "editable with live default" choice for this specific trigger — confirmed explicitly). Refactored `quality_inspection.js`: extracted `_desar_fetch_baseline`/`_desar_is_packing` helpers, added a `quality_inspection_template(frm)` handler that re-fetches the baseline and force-recomputes via the existing `_desar_recompute` (which already always overwrites) — no Python changes needed.
- [x] **User asked whether Save (not just Submit) validates grade movement — it didn't.** `before_submit` only fires on the docstatus 0→1 transition; a plain Save never ran `_validate_grade_adjustments`. Extracted the shared guard (non-empty readings, non-zero total, is this Design Master's final stage) into `_final_stage_readings(doc)`; added a new `validate(doc, method)` hook (registered in `hooks.py`) that runs the existing, unchanged `_validate_grade_adjustments` on every save; `before_submit` now only keeps its own total-vs-WO-qty check and no longer redundantly re-calls the adjustment check. New `TestValidateOnSave` (4 tests, patches `is_final_stage`/`get_design_master_from_qi`/`get_stage_grades_from_rt` since this is a unit test, not a full Design Master fixture) confirms the real `validate()` throws on mismatch and passes when reconciled. Flagged to the user: this now blocks *every* intermediate Save, not just Submit — accepted as intended. Full suite 233/233.
- [ ] **Not verified end-to-end in a browser** — on a Packing QI, enter a Grade A/B/C value that doesn't reconcile with Finishing and no Grade Adjustment rows, click Save (not Submit), confirm it now throws immediately; add a matching adjustment row and Save again, confirm it saves cleanly.

## Full codebase audit + fix pass (this session)

Six parallel research agents audited hooks/events, services, doctype controllers, API security, test coverage, and fit against native ERPNext v15 manufacturing. Findings ranked and fixed in priority order:

- [x] **Page-controller RPCs had zero auth, unlike `api/*.py`.** The earlier "restrict manufacturing API access" commit only hardened `api/manufacturing.py` and `api/production_order.py` — `desar_production_controller.py`, `desar_production_workspace.py`, `desar_supervisor.py` were untouched. Any authenticated user could force-start/complete any Job Card or force-complete any Work Order's stage. Added `frappe.only_for(...)` to every sensitive endpoint across all three files, new shared `constants/roles.py` (`ALL_DESAR_ROLES`/`SUPERVISOR_ROLES`/`JOB_CARD_ROLES`/`QC_ROLES`/`STORE_ROLES`), and `_guard_job_card_owner()` so a non-supervisor can only start/complete their own assigned Job Card. Full suite 233/233.
- [x] **Cancelled Production Orders still accepted stage actions.** None of `roll_service.py`'s 8 stage-transition functions or `warping_service.py`'s 3 checked `po.docstatus`. Added `_guard_po_submitted()` to all 11.
- [x] **Cancel-path reconciliation had no locking.** `revert_qi_reference`/`revert_stock_entry_reference`/`revert_work_order_reference` read via unlocked `frappe.get_all` and mutated without `for_update`, unlike every forward-path function. Now lock the parent PO per row before mutating; removed the now-dead `_refresh_po_status_by_name` helper.
- [x] **Grade qty fields could go negative**, gaming the strict total-vs-WO-qty check. Added `non_negative: 1` to `DESAR QI Grade Reading.qty` and `DESAR Roll Ticket Stage Grade.qty` (matches `DESAR Grade Adjustment`, which already had it). Migrated clean.
- [x] **Blank/zero `valuation_pct` silently zeroed Grade B/C/etc. valuation** in dynamic-mode repack. `repack_service.py` now throws before building that item row instead of computing `basic_rate=0`. Legacy mode uses a hardcoded ratio constant, unaffected.
- [x] **`batch_service.get_batch_from_stock_entry` Method-3 SLE lookup missing `is_cancelled: 0`** (present in the equivalent `stock_entry_repository.py` query) — could return a stale batch off an amended/resubmitted SE. Added the filter.
- [x] **QI cancel didn't enforce Repack SE reversal**, just an easy-to-miss `msgprint`. Added `_guard_repack_se_not_submitted()` to `quality_inspection.on_cancel` — blocks cancelling a Final Packing QI while its linked Repack SE (via `custom_source_qi`) is still submitted.
- [x] **`desar_beam_loom_row/` was dead** — only a stale `.pyc` cache left from a deleted source file, not git-tracked, zero references, no DB DocType record. Deleted.
- [x] **Deduped `_is_submitted`** (was copy-pasted identically in `roll_service.py` and `warping_service.py`) into `utils/doc_utils.is_submitted`. Deduped the inline batch-from-SE-row extraction in `warping_service._create_repack_se` to reuse `utils/batch_utils.get_batch_from_row` instead of a third hand-rolled copy.
- [x] **Attempted, then reverted: linking Repack SE to `work_order`** for WO-costing visibility. ERPNext v15 core (`stock_entry.py::validate_work_order`) unconditionally sets `self.work_order = None` for any purpose other than Manufacture-family/Material Transfer — Repack is neither, so the field never survives `validate()`. Even if it did, `validate_finished_goods()` would then throw "Finished Item does not match Work Order" for every grade-suffixed item row, since none equal the WO's `production_item`. **This is a hard ERPNext core constraint, not a DESAR gap — do not re-attempt via the native field.** The QI→WO traceability already exists via `custom_source_qi` → `get_work_order_from_qi()`; any report needing Repack-to-WO linkage should join through that chain instead.
- [ ] **Multi-company / single-warehouse-set limitation — accepted, not fixed.** `DESAR Settings` is a Single doctype with one hardcoded company + one warehouse per stage; `install/setup.py` defaults to `"Standardtouch"` / `"- ST"`-suffixed warehouses. Confirmed with the user this factory has no near-term multi-company need. If that changes, `DESAR Settings` needs restructuring into a child table keyed by company — real schema migration, plan separately.
- [ ] **Deferred: `roll_service.py` (624 lines) file split** and unifying the two independently-maintained warehouse-fallback keyword tables (`events/work_order.py::_get_warehouses_for_stage` vs `roll_service.py::_configure_wo`, different keyword sets — e.g. "grey" isn't recognized by the event hook's table at all, falls through to a generic "Work In Progress - ST" fallback). User chose dedupe-only scope this round; this specific merge touches live warehouse routing and needs real Stage Configuration data to verify safely before changing — flagged for a dedicated pass, not done blind.

## Fix: Production Order list showed "Submitted" for everything (see `specs/production-order-stage-visibility.md`)

- [x] **Root cause: `warping_service.py` never refreshed PO status.** Only `roll_service.py`'s 13 stage-transition points called `_refresh_po_status()`; `start_warping`/`complete_warping`/`split_beam` set `warping_status`/`beam_split_status` via `db_set` but never triggered a recompute, so `status` sat on "Submitted" for the entire Warping + Beam Split phase (usually the longest-visible stretch). Added the missing calls.
- [x] **Root cause 2: even when refreshed, `status` only has 3 usable buckets** (Submitted/In Progress/Completed) — can't show *which* stage (Warping/Grey Roll/Finished Roll/Packing) a roll is at, even though that data already exists and is rendered correctly on the form dashboard (`desar_production_order.js`). Added new read-only `current_stage` field (`in_list_view: 1`) computed by new `services/stage_status_service.py` (`refresh_stage_status`), reusing the same stage precedence the form JS already uses. Multi-roll orders at different stages show grouped counts, e.g. `"Grey Roll: 2, Packing: 1"`, instead of enumerating every roll.
- [x] De-duped: `roll_service._refresh_po_status` now just calls the shared `stage_status_service.refresh_stage_status` — one status-computation implementation instead of the risk of two drifting apart.
- [x] `patches/v3_2_backfill_current_stage.py` backfills `current_stage` on already-submitted orders. Verified against real data on `excel`: e.g. `DESAR-PO-2026-00017` (mid-cycle) now shows `current_stage: "Grey Roll"` instead of a generic status; `DESAR-PO-2026-00025` (finished) shows `"Completed"`.
- [x] New `TestCurrentStageComputation` (8 tests) in `tests/test_production_order.py`, including a direct regression test for the "stuck on Submitted during Warping" bug. Full suite: 292/292 passing on `excel`.
- [x] **User asked whether folding `current_stage` into `status` itself (instead of a second column) was the better call.** Recommended against: `status` is Select-typed and `before_submit`/`on_cancel` hardcode it to exact strings "Submitted"/"Cancelled" — widening its options to free-form stage text risks those hooks and anything else matching on the literal value, for no real gain (would still need a rewrite of when those hooks fire to avoid a stale "Submitted" flash right after submit). User agreed; kept `current_stage` separate but added `desar_production_order_list.js` (`frappe.listview_settings["DESAR Production Order"].formatters.current_stage`) so the column renders as a colored indicator pill (Warping=gray, Beam Split=blue, Grey Roll=orange, Finished Roll=yellow, Packing=purple, Completed=green, mixed multi-roll strings=cyan) — same visual treatment as `status` without touching its field type or hooks. `bench build --app desar_manufacturing` ran clean.
- [ ] **Not click-tested in a live browser** — no browser/agent-browser tool available this session. Before closing out: open the Production Order list view on `excel`, confirm the new "Current Stage" column renders as a colored pill (not plain text) and updates in real time as a live order progresses through Warping → Beam Split → per-roll stages.

## Onboarding cues on the Production Order form (user couldn't tell what to click next)

- [x] Kept existing button layout/grouping exactly as-is (user's explicit choice — no promotion to a primary top-right button). Added, in `desar_production_order.js` only: `@keyframes desar-pulse`/`.desar-pulse` CSS glow on every actionable button (the existing conditionals in `render_warping_actions`/`render_beam_split_actions`/`_add_roll_buttons` already render exactly one valid action at a time, so no new "is this next" logic was needed — just tagged each at creation); hover-tooltip hints via `title` attr on the same buttons; a `frm.set_intro()` banner (`render_next_step_guidance` trigger, `_next_step_message`/`_roll_next_action_label` helpers) stating the single next action in plain English, recomputed on every refresh and again once dashboard data (QI status) loads.
- [ ] **User asked for a scoped-revert guarantee up front**: if this doesn't look good and they say "revert animation," only this addition (the CSS pulse block, the `title`/`.desar-pulse` tagging in `_btn`/`_pulse_custom_button`, and the `render_next_step_guidance`/`_next_step_message`/`_roll_next_action_label` block) should be undone — nothing else in this file or elsewhere touched. Noted here so a future session honors that scope even without the original conversation.
- [ ] **Not click-tested in a live browser** — no browser tool available this session. Before closing out: open a Production Order at each stage (pre-Warping, mid-Warping, post-Beam-Split, per-roll Grey/Finished/Packing) and confirm (a) the intro banner text matches the actual pending action, (b) exactly the right button pulses, (c) hovering shows the hint tooltip.
- [x] **User said the pulse color didn't grab attention.** Scoped fix, same block only: pulse changed from a blue glow (blended into `.btn-primary`'s own blue) to solid orange (`#f97316`) fill + glow ring + a slight scale bump, faster cycle (1.2s).
- [x] **User: pulse was "not accurate"** — only the button pulsed, not the Job Cards/Stock Entries/Roll Ticket a new user actually needs to act on next (e.g. after Start Grey Roll, the open Job Cards need attention, not just the eventual Complete Grey Roll button). Asked via AskUserQuestion to scope: highlight Job Cards + Stock Entries + Roll Ticket links (all three, recommended); investigate both stranding paths (`View Transfer/Repack SE` navigates away same-tab, badge links open a new tab); on return, a "Back to Production Order" button/banner rather than auto-redirect.
  - New lighter `.desar-pulse-ring` (glow ring only, no forced background — preserves each badge's own status color) applied to: non-Completed Job Card badges, Draft Stock Entry rows, the Roll Ticket link while its roll isn't yet Completed. The existing solid `.desar-pulse` now also tags the "Submit SE"/"Submit QI" buttons (the actual next click for a Draft item), not just the 6 stage-action buttons from before.
  - New `stage_status_service.find_production_order_for_stock_entry()` (services/stage_status_service.py) reverse-resolves any DESAR-owned Stock Entry back to its Production Order — direct field match for `warping_transfer_se`/`warping_manufacture_se`/`beam_split_se` (on the PO) and `repack_se`/`packing_manufacture_se` (on `DESAR Roll Chain`); falls back to matching the Stock Entry's own `work_order` against `warping_wo`/`grey_roll_wo`/`finished_roll_wo`/`packing_wo` for the Grey/Finished/Packing stage SEs, which aren't stored by name anywhere (the roll dashboard already fetches those live by work_order, same pattern reused here). Exposed as `api.production_order.find_production_order_for_stock_entry` (permission-checked against the Stock Entry itself, otherwise ungated like this app's other read-only info endpoints). Verified against real data on `excel`: transfer SE, manufacture SE, and packing manufacture SE all resolved to their correct Production Orders.
  - New `frappe.ui.form.on("Stock Entry", ...)` block added to the existing global `public/js/desar.js` (already loaded on every desk page via `app_include_js` — no new hooks.py registration needed): adds a "Back to Production Order" button (group "DESAR", same convention as every other custom button in this app) plus a `frm.set_intro()` banner with a direct link, whenever the open Stock Entry resolves to a DESAR Production Order. No auto-redirect, per user's choice.
  - Full suite still 292/292 after these changes; `bench build` clean.
- [ ] **Not click-tested in a live browser.** Before closing out: (a) start Grey Roll on a real roll, confirm the resulting Job Card badge pulses until its Job Card is completed; (b) open a Draft Transfer/Manufacture SE from the roll dashboard, confirm it and its Submit button both pulse; (c) submit a Stock Entry from its own full form (via "View Transfer SE") and confirm the "Back to Production Order" button/banner appears and correctly routes back.
- [x] **User: pulse wasn't sequenced right** — Job Card badge and "Complete Grey Roll" pulsed at the same time, even though the server (`roll_service._validate_job_cards`) actually rejects Complete while any Job Card for that stage's work order isn't Completed. New `_job_cards_pending(frm, work_order)` mirrors that same check client-side; "Complete Grey Roll"/"Complete Finished Roll"/"Complete Packing" only pulse (and go blue/primary) once it returns false — otherwise they render as a plain grey button with a "Complete the open Job Card(s) above first" hover hint, so only one thing pulses at a time. `_next_step_message`'s banner text updated the same way. Full suite 292/292, build clean.
- [x] **User: remove the inline "Submit QI"/"Submit SE" quick-action buttons** — workflow should force going into the actual Quality Inspection/Stock Entry document to fill required details before submitting there, not a one-click submit from the Production Order dashboard. Removed both buttons (`_qi_badge_html`, `_se_badge_html`) and the now-dead `_bind_submit_buttons`/`_submit_doc` (and their 2 call sites in `render_warping_dashboard`/`render_roll_chains`) — the badge/link and its `.desar-pulse-ring` nudge stay, so a Draft item still visibly pulses, it just always opens the real document instead of offering a shortcut submit.
- [x] **User: pulse showed stale state** — completing a Job Card on its own form (opened via `target="_blank"`, a separate tab) never refreshed the original Production Order tab, so its badge kept pulsing until a manual page reload. Added a `focus.desar_po`-namespaced `window` focus listener (re-bound, not stacked, on every `refresh`) that re-triggers `load_and_render_dashboard` — re-fetches Job Cards/Stock Entries/QIs — whenever the user tabs back into this Production Order. Full suite 292/292, build clean.
- [x] **User: Roll Ticket pulse not wanted** — scoped removal, just that one span's `.desar-pulse-ring` class in `_render_roll_row`; badge/link/animation elsewhere untouched.
- [x] **User: no guidance on the Quality Inspection form itself after submitting, and returning to the Production Order still didn't refresh.** Two separate fixes:
  - Added `find_production_order_for_quality_inspection` (`stage_status_service.py`, exposed via `api/production_order.py`) — reuses the existing Stock Entry lookup, since `qi_service.make_roll_qi` already always points `reference_type`/`reference_name` at the roll's Manufacture SE. Wired into the existing dedicated `public/js/quality_inspection.js` (`_desar_show_production_order_link`, called from its `refresh`) — same "Back to Production Order" button + `set_intro` banner pattern as Stock Entry. Verified against real data on `excel`.
  - The window-focus listener on the Production Order form (added in the previous round) was only re-fetching the Job Card/SE/QI *dashboard cache*, not `frm.doc` itself — a submitted QI can change the roll's own `roll_chains` fields server-side, which stayed stale until a manual page reload. Upgraded that listener to a full `frm.reload_doc()` (which re-fires `refresh`, re-running the dashboard fetch too) instead of just `load_and_render_dashboard`.
  - Full suite 292/292, build clean.
- [ ] **Not click-tested in a live browser.** Before closing out: submit a roll-stage QI from its own form, confirm the "Back to Production Order" button/banner appears there, and confirm the Production Order tab reflects the change (no stale pulse, no stale roll status) after switching back to it without a manual reload.
